<% if @user.errors.any? %>
    <div class="alert alert-danger" role="alert">
        <h2><%= pluralize(@user.errors.count, "error") %> prohibited this <%= @user.email%> from being saved:</h2>
        <ul>
            <% @user.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
        </ul>
    </div>
<% end %>